%h2 Client List
-if @auth.is_admin || @auth.is_pm
  #new_btn= link_to('New Client', new_client_path, :remote => true, :class => 'button radius')
#new_client
%table#clients
  = render(:partial => 'clients')